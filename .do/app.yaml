name: publishr
region: lon
services:
- dockerfile_path: src/Publishr.Web/Dockerfile
  github:
    branch: main
    deploy_on_push: true
    repo: da1rren/Publishr
  http_port: 80
  instance_count: 1
  instance_size_slug: basic-xxs
  name: publishr-web
  routes:
  - path: /
  health_check:
    http_path: /health
  source_dir: src/
